//Creating Table Contacts

CREATE TABLE contacts (
  contact_id INTEGER,
  name VARCHAR(200),
  email VARCHAR(200),
  phoneno INTEGER,
  address VARCHAR(200),
  time DATETIME
)
  
INSERT INTO contacts (
  VALUES(
